apiVersion: v1
kind: Pod
metadata:
  name: "{{ template "fullname" . }}-web-selenium-test"
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: {{ template "fullname" . }}-web-selenium-test
      image: "oveits/crochunter-tests:PR-6"
      env:
        - name: SELENIUM_HUB_URL
          value: "http://dev-node1.vocon-it.com:31881/wd/hub"
        - name: INGRESS_HOSTNAME
          value: "crochunter.vocon-it.com"
      #command: ["curl"]
      #args: ["retry 3", "retry-delay 10", "{{ template "fullname" . }}:{{.Values.servicePort}}"]
  restartPolicy: Never